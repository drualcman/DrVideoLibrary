{
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": [ "/static/*", "/api/*", "/images/*.{png,jpg,gif}", "/css/*", "/_framework/*", "/_content/*" ]
  },
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": [ "anonymous" ]
    }
  ]
}
