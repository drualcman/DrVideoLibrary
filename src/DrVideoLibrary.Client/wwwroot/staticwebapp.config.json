{
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": [ "/static/*", "/api/*", "/images/*.{png,jpg,gif}", "/css/*" ]
  },
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": [ "anonymous" ]
    }
  ]
}
